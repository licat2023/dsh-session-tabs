/**
 * dsh-session-tabs — host loader entry for the browser-only plugin.
 * Provides no host-side behavior; the browser half (lib/client.js) renders
 * the tab bar through the shell.overlay slot.
 */
function apply() {}
export { apply }
